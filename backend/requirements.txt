# JSpark Recommendation Engine — Python Dependencies
# Install: pip install -r requirements.txt

# Core data
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.2
pyarrow==16.1.0
python-dateutil==2.9.0

# Retrieval (primary — requires internet to download model on first run)
sentence-transformers==3.0.1
# chromadb==0.5.0
chromadb>=0.6.0

# Retrieval (fallback — always available, no download required)
scikit-learn==1.5.0

# LLM layer
openai==1.35.0          # OpenRouter uses OpenAI-compatible API

# Orchestration
python-dotenv==1.0.1

# API Server
fastapi==0.111.0
uvicorn==0.30.1

# Datacube generation (generate_datacube.py only)
# Already covered by pandas + openpyxl above
